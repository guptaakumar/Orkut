<ul>
  <% @amigos.each do |amigo| %>
    <li><%= link_to image_tag(amigo.avatar.url(:micro)), amigo %> - <%= amigo.nome %> <%= link_to('+ Amigo') if perfil_atual.amizades.pode_adicionar?(amigo) %> </li>
  <% end %>
</ul>
